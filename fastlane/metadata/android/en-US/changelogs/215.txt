More reliable automatic location — now uses Wi‑Fi/network alongside GPS, so it won't get stuck when GPS is unavailable indoors, and it works with Android's "Approximate location". Prayer times switch correctly as you travel between countries: the Swedish and global sources swap across borders, and returning to a recent city reuses its saved times. The location chip now shows your city in automatic mode too. Faster first‑time Quran page download.
