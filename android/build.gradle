buildscript {
    ext {
        buildToolsVersion = "37.0.0"
        minSdkVersion = 24
        // compileSdk 37 so the Android 17 Live Update APIs (MetricStyle,
        // Semantic Color, direct setRequestPromotedOngoing) compile. targetSdk
        // stays 36 to avoid unrelated API-37 behaviour changes for now.
        compileSdkVersion = 37
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
        // Google Play policy: all apps must use Play Billing Library >= 8.0.0
        // by 2026-08-31. react-native-iap (play flavor only — the tip jar)
        // defaults to 7.0.0 but honours this rootProject override; its
        // 12.16.x native code already uses the modern ProductDetails APIs, so
        // it compiles and runs against 8.x unchanged.
        playBillingSdkVersion = "8.0.0"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

